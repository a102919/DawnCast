# DawnCast 前端環境變數

# 後端 FastAPI base URL（http 模式時必填），例：http://localhost:8000
VITE_API_BASE_URL=

# Supabase（僅用於 Auth 取 JWT；http 模式必填）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# 是否使用 mock 資料。預設 true（離線示範，免登入）。
# 設為 'false' 才會接真實後端 API。
VITE_USE_MOCK=true
