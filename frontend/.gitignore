# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 環境變數（含密鑰），勿提交
.env
.env.*
!.env.example
# 例外：prod 公開設定可提交（Supabase URL / anon key 是 browser-public，
# 不是 secret）。首次部署後若 API host 不是 dawncast-api.fly.dev，記得改。
!.env.production

# 後端 OpenAPI 匯出（由 backend/scripts/export_openapi.py 產生，build input，
# 不是事實來源本身——事實來源是 backend/shared/models.py）。
# 產生的 src/api/generated.ts 要 commit，這份原始 JSON 不用。
openapi.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
