{
  "name": "dawncast-audio-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: R2 audio proxy with correct Range/206/Content-Range handling for Safari",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}